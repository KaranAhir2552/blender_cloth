[pytest]
testpaths = tests
markers =
    pure: pure-Python tests (no Blender, no mock)
    mock: tests running against the strict mock bpy
    static: static validation (compile, imports, lint, API contract, schemas)
addopts = -p no:cacheprovider
